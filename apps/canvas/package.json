{
  "name": "@agent-canvas/canvas",
  "version": "0.0.0",
  "private": true,
  "description": "Multiplayer canvas client built on tldraw with custom AgentShape rendering.",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "tsc -b && vite build",
    "typecheck": "tsc -b --pretty",
    "test": "vitest run",
    "preview": "vite preview",
    "e2e": "playwright test",
    "e2e:install": "playwright install --with-deps chromium"
  },
  "dependencies": {
    "@agent-canvas/orchestrator-types": "*",
    "react": "^19.2.7",
    "react-dom": "^19.2.7",
    "tldraw": "^5.1.0"
  },
  "devDependencies": {
    "@playwright/test": "^1.49.0",
    "@types/react": "^19.2.16",
    "@types/react-dom": "^19.0.0",
    "@vitejs/plugin-react": "^6.0.2",
    "typescript": "^6.0.3",
    "vite": "^8.0.16",
    "vitest": "^4.1.8"
  }
}
